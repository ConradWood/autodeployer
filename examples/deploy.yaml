---

#####################################################
# THIS DEPLOYMENT YAML was generated by skel-go
# it should work as-is
# DO NOT replace it or modify it unless you know
# what you are doing and are troubleshooting,
# debugging or otherwise interfacing with the
# CI/CD system
#####################################################


namespace: panasonic
groups:
 - groupid: testing
   applications:
    - repository: panasonic
      binary: dist/linux/amd64/panasonic-server
      downloadurl: http://buildrepo/builds/panasonic/master/${BUILDID}/dist/dist.tar
      instances: 1
      critical: true
      alwayson: true
      machines: johnsmith
      args:
       - -registry=registry
       - -port=${PORT1}
